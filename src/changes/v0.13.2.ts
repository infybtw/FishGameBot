import type { Change } from "./types.ts";

export const change: Change = {
  version: "0.13.2",
  updates: ["Проклятие тяжёлой сети теперь корректно удваивает кулдаун, сокращённый событием «Штиль»."],
};
