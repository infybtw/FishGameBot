import type { Change } from "./types.ts";

export const change: Change = {
  version: "0.13.1",
  updates: ["Команда /changelog теперь показывает версии постранично; подробности открываются кнопкой только для вызвавшего команду игрока."],
};
