import type { Change } from "./types.ts";

export const change: Change = {
  version: "0.11.5",
  updates: ["Добавлена команда /fishtop_text с текстовой версией топа для ников со специальными символами."],
};
