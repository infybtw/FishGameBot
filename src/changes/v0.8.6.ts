import type { Change } from "./types.ts";

export const change: Change = {
  version: "v0.8.6",
  updates: ["Добавлена команда /event_stop для остановки текущего события до его следующего запуска."],
};
