import type { Change } from "./types.ts";

export const change: Change = {
  version: "v0.10.1",
  updates: ["Меню /fish_upgrade теперь видно только владельцу, а результат улучшения публикуется в чате с упоминанием игрока."],
};
