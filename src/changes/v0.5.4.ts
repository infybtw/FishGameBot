import type { Change } from "./types.ts";

export const change: Change = {
  version: "v0.5.4",
  updates: ["Бот сохраняет отправителей сообщений для более точного отслеживания активности в чате."],
};
