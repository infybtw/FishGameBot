import type { Change } from "./types.ts";

export const change: Change = {
  version: "0.11.8",
  updates: ["Исправлена работа /trade: настройки обмена видны только инициатору, а предложение - всем в чате."],
};
