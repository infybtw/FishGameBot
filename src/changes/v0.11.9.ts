import type { Change } from "./types.ts";

export const change: Change = {
  version: "0.11.9",
  updates: ["При перезапуске бот больше не отвечает на команды, накопившиеся во время его офлайна."],
};
