import type { Change } from "./types.ts";

export const change: Change = {
  version: "v0.8.7",
  updates: ["С 06:00 до 08:00 действует событие «Сбой снастей»: бафы экипированной удочки не работают."],
};
